export type Experience = {
  role: string;
  company: string;
  period: string;
  description: string;
};

// TODO: reemplazar con datos reales de experiencia laboral (empresa, puesto, fechas, descripción).
// La sección se oculta automáticamente si este arreglo está vacío.
export const experience: Experience[] = [];
